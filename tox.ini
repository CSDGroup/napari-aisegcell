# For more information about tox, see https://tox.readthedocs.io/en/latest/
[tox]
envlist = py{312,313}-{linux,macos,windows}
isolated_build=true

[gh-actions]
python =
    3.12: py312
    3.13: py313

[gh-actions:env]
PLATFORM =
    ubuntu-latest: linux
    macos-latest: macos
    windows-latest: windows

[testenv]
platform =
    macos: darwin
    linux: linux
    windows: win32
passenv =
    CI
    GITHUB_ACTIONS
    DISPLAY
    XAUTHORITY
    NUMPY_EXPERIMENTAL_ARRAY_FUNCTION
    PYVISTA_OFF_SCREEN
deps =
    pytest  # https://docs.pytest.org/en/latest/contents.html
    pytest-cov  # https://pytest-cov.readthedocs.io/en/latest/
    pytest-xvfb ; sys_platform == 'linux'
    py{312,313}-linux: --extra-index-url=https://download.pytorch.org/whl/cpu
    py{312,313}-linux: torch==2.9.0+cpu
    py{312,313}-linux: torchvision==0.24.0+cpu
    {macos,windows}: torch == 2.9.0
    {macos,windows}: torchvision == 0.24.0
    lightning == 2.5.5
extras =
    dev
commands = pytest -v --color=yes --cov=napari_aisegcell --cov-report=xml
